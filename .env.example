# Base services
# Use strong, unique values in your real .env on the droplet. Do NOT commit secrets.

# Database
# Only used for local/dev overrides. In Compose, the backend already receives DATABASE_URL.
# If you want the app to read from this file, set a proper URL here or rely on Compose env.
# Example format: postgresql+psycopg2://<user>:<pass>@<host>:<port>/<db>
# MSSQL (Azure SQL) examples:
# 1) Simple URL form (driver name must be URL-encoded with spaces as '+')
# DATABASE_URL=mssql+pyodbc://<user>:<pass>@seoanalyzer.database.windows.net:1433/SEO?driver=ODBC+Driver+18+for+SQL+Server&Encrypt=yes&TrustServerCertificate=no&Connection+Timeout=30
# 2) odbc_connect form (entire ODBC string URL-encoded)
# DATABASE_URL=mssql+pyodbc:///?odbc_connect=Driver%3DODBC+Driver+18+for+SQL+Server%3BServer%3Dtcp%3Aseoanalyzer.database.windows.net%2C1433%3BDatabase%3DSEO%3BUid%3D<user>%3BPwd%3D<pass>%3BEncrypt%3Dyes%3BTrustServerCertificate%3Dno%3BConnection%20Timeout%3D30%3B
#DATABASE_URL=
POSTGRES_PASSWORD=change-me

# Redis
# Example: redis://<host>:<port>/<db-index>
REDIS_URL=redis://redis:6379/0

# Application secrets (leave blank in the example)
OPENAI_API_KEY=
GOOGLE_API_KEY=
PERPLEXITY_API_KEY=
SERPAPI_KEY=

# Frontend (Vite)
# Base path the frontend will use to reach the API.
# In dev, vite.config.ts defaults to '/api' if not provided.
VITE_API_BASE=/api

# CORS configuration for the API
# Comma-separated list of allowed origins. Example for prod:
# CORS_ORIGINS=https://app.example.com,https://www.example.com
CORS_ORIGINS=

