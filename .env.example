# Base services
# Use strong, unique values in your real .env on the droplet. Do NOT commit secrets.

# Database
# Only used for local/dev overrides. In Compose, the backend already receives DATABASE_URL.
# If you want the app to read from this file, set a proper URL here or rely on Compose env.
# Example format: postgresql+psycopg2://<user>:<pass>@<host>:<port>/<db>
#DATABASE_URL=
POSTGRES_PASSWORD=change-me

# Redis
# Example: redis://<host>:<port>/<db-index>
REDIS_URL=redis://redis:6379/0

# Application secrets (leave blank in the example)
OPENAI_API_KEY=
GOOGLE_API_KEY=
PERPLEXITY_API_KEY=
SERPAPI_KEY=

# Frontend (Vite)
# Base path the frontend will use to reach the API.
# In dev, vite.config.ts defaults to '/api' if not provided.
VITE_API_BASE=/api

# CORS configuration for the API
# Comma-separated list of allowed origins. Example for prod:
# CORS_ORIGINS=https://app.example.com,https://www.example.com
CORS_ORIGINS=

